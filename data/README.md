# 数据文件说明

本目录包含宝可梦对战系统的所有静态数据文件。

## 📁 文件结构

```
data/
├── pokemon.js      # 宝可梦数据（属性、技能）
├── types.js        # 属性系统数据（相克表、名称、颜色）
└── README.md       # 本说明文档
```

## 📄 文件详情

### `pokemon.js` - 宝可梦数据

包含所有可用宝可梦的基础数据：

**数据结构：**
```javascript
{
  id: number,           // 宝可梦图鉴编号
  name: string,         // 宝可梦名称
  type: string[],       // 属性类型数组（单属性或双属性）
  hp: number,           // 生命值
  attack: number,       // 攻击力
  defense: number,      // 防御力
  speed: number,        // 速度
  skills: Skill[]       // 技能列表
}
```

**技能数据结构：**
```javascript
{
  name: string,         // 技能名称
  type: string,         // 技能属性
  power: number,        // 威力（0表示变化技能）
  accuracy: number,     // 命中率（百分比）
  priority: number,     // 优先度（数值越大越先出手）
  effect?: {            // 可选的附加效果
    type: string,       // 效果类型（'status' 状态异常 / 'stat' 能力变化）
    status?: string,    // 状态异常类型（poison, burn, paralysis等）
    stat?: string,      // 能力变化目标（attack, defense, speed等）
    change?: number     // 能力变化等级（-6 到 +6）
  }
}
```

**当前包含的宝可梦：**
- #001 妙蛙种子（草系/毒系）
- #004 小火龙（火系）
- #007 杰尼龟（水系）
- #025 皮卡丘（电系）

### `types.js` - 属性系统数据

包含三个核心数据对象：

#### 1. `typeMatchups` - 属性相克表

定义属性之间的克制关系：

**倍率说明：**
- `2.0`: 效果拔群（克制）
- `1.0`: 普通效果（默认，不在表中列出）
- `0.5`: 效果不佳（抵抗）
- `0.0`: 完全无效（免疫）

**双属性计算：**
- 最终倍率 = 属性1倍率 × 属性2倍率
- 例如：水系+飞行系被电系攻击 = 2 × 2 = 4倍伤害

#### 2. `typeNames` - 属性中文名称映射

将英文属性代码映射为中文显示名称。

**示例：**
```javascript
{
  fire: '火系',
  water: '水系',
  grass: '草系',
  // ...
}
```

#### 3. `typeColors` - 属性颜色映射

为每个属性定义 Tailwind CSS 渐变类，用于UI显示。

**示例：**
```javascript
{
  fire: 'from-red-500 to-orange-600',
  water: 'from-blue-500 to-cyan-600',
  // ...
}
```

## 🔧 使用方法

### 在 HTML 中引入

在 `battle-optimized.html` 中按以下顺序加载：

```html
<!-- 1. 先加载数据文件 -->
<script src="data/pokemon.js"></script>
<script src="data/types.js"></script>

<!-- 2. 再加载核心系统 -->
<script src="systems/core/EventBus.js"></script>
<script src="systems/core/MicroTurnScheduler.js"></script>

<!-- 3. 最后加载主逻辑 -->
<script src="battle-enhanced-micro.js"></script>
```

### 在 JavaScript 中使用

数据会自动暴露到全局作用域：

```javascript
// 访问宝可梦数据
const pikachu = pokemonData[25];
console.log(pikachu.name); // "皮卡丘"

// 访问属性相克表
const effectiveness = typeMatchups.fire.grass;
console.log(effectiveness); // 2 (火克制草)

// 访问属性名称
const typeName = typeNames.electric;
console.log(typeName); // "电系"

// 访问属性颜色
const typeColor = typeColors.water;
console.log(typeColor); // "from-blue-500 to-cyan-600"
```

## ➕ 添加新数据

### 添加新宝可梦

在 `pokemon.js` 中添加新条目：

```javascript
const pokemonData = {
    // ... 现有宝可梦 ...
    
    6: { 
        id: 6, 
        name: '喷火龙', 
        type: ['fire', 'flying'], 
        hp: 78, 
        attack: 84, 
        defense: 78, 
        speed: 100, 
        skills: [
            // ... 技能列表 ...
        ]
    }
};
```

### 扩展属性相克表

在 `types.js` 中添加新属性的克制关系：

```javascript
const typeMatchups = {
    // ... 现有属性 ...
    
    fighting: {
        normal: 2,
        ice: 2,
        rock: 2,
        dark: 2,
        steel: 2,
        poison: 0.5,
        flying: 0.5,
        psychic: 0.5,
        bug: 0.5,
        fairy: 0.5,
        ghost: 0
    }
};
```

## 📝 注意事项

1. **加载顺序**：数据文件必须在主逻辑文件之前加载
2. **全局变量**：所有数据都暴露在全局作用域，注意命名冲突
3. **数据一致性**：确保宝可梦的 `type` 字段与 `typeMatchups` 中的键匹配
4. **浏览器兼容性**：使用传统方式，兼容所有现代浏览器

## 🔮 未来改进

- [ ] 添加更多宝可梦数据
- [ ] 完善属性相克表（添加更多属性）
- [ ] 添加道具数据文件 `items.js`
- [ ] 添加特性数据文件 `abilities.js`
- [ ] 考虑迁移到 ES6 模块系统
- [ ] 添加数据验证工具

